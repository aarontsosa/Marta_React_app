import React from 'react';

const Destination = ({destination}) => {
    // let newDest = destination.split(" ")
    // newDest = newDest.slice(0, (newDest.length-1))
    return <h3 className="class-title">{destination}</h3>
}

export default Destination